numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
PyYAML>=6.0

# frontend (app/)
dash>=2.16
plotly>=5.18

# optional
#
# asammdf reads the bench .mf4 log: the Operating map's temperature, pressure
# and humidity fields, and the operating point printed beside each whole-cell
# comparison. Everything else works without it, so it is NOT in this file --
# asammdf unconditionally depends on the PyPI package "zstd" (a different
# project from "zstandard"), which stopped publishing Windows wheels after
# Python 3.10. On Python 3.11+ on Windows, `pip install asammdf` tries to
# compile zstd from source and fails with "Microsoft Visual C++ 14.0 or
# greater is required" unless the machine has that compiler installed -- so
# putting it in this file would break `pip install -r requirements.txt` for
# most current Windows setups, taking every other package down with it.
#
# Install it with:
#   pip install --no-deps asammdf
#   pip install chardet "canmatrix[arxml,dbc]" lxml lz4 numexpr python-dateutil typing-extensions zstandard
# `local_eis/_vendor/zstd.py` then stands in for the real "zstd" package,
# using zstandard (which does ship Windows wheels) to provide the one
# function asammdf actually imports from it. See that file for why.
pyarrow>=12.0                  # Parquet output; without it the pipeline writes CSV
databricks-sql-connector>=3.0  # datago source; not needed for file-system results
gunicorn>=21.0                 # production server for the Dash app
pytest>=7.0                    # test suite
